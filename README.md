# dict-lookup
Bash script to query dictionaryAPI or Usito (French Canadian) end sending the
word definition to the notification daemon

## Dependencies
- `bash 4+`
- `jq`
- `htmlq`
- `X11` with `xclip` & `setxkbmap`

**Optional:**
- A Nerd Font set as you notification daemon default
